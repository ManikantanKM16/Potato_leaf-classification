# 🌿 PotatoShield — Free Deployment Guide

> Deploy at **`https://potatoshield.vercel.app`** — 100% free, no credit card.

| Service | What it hosts | Cost |
|---------|--------------|------|
| [Vercel](https://vercel.com) | React frontend | **Free forever** |
| [Render](https://render.com) | FastAPI backend + ML model | **Free forever** |
| [GitHub](https://github.com) | Code storage | **Free forever** |

> ⚠️ **Domain note:** Domain names (`.com`, `.in`, etc.) always cost money (~$10/year). Your free URL will be `https://potatoshield.vercel.app`. If you want a real domain later, just buy one and point it to Vercel — takes 5 minutes.

> ⚠️ **Cold start note:** Render's free tier sleeps after 15 minutes of inactivity. The first request after sleeping takes ~30–60 seconds to wake up. All subsequent requests are instant.

---

## Step 1 — Push Code to GitHub

1. Create a free account at [github.com](https://github.com)
2. Create a **new repository** named `potatoshield` (set to Public)
3. On your Windows PC, open PowerShell inside the project folder and run:

```powershell
cd C:\Users\91990\.gemini\antigravity\scratch\leaf-classification
git init
git add .
git commit -m "Initial commit"
git branch -M main
git remote add origin https://github.com/YOUR_USERNAME/potatoshield.git
git push -u origin main
```

> Replace `YOUR_USERNAME` with your actual GitHub username.

---

## Step 2 — Deploy Backend to Render

1. Go to [render.com](https://render.com) and sign up (free, no credit card)
2. Click **"New +"** → **"Web Service"**
3. Connect your GitHub account and select your `potatoshield` repo
4. Fill in the settings:

   | Setting | Value |
   |---------|-------|
   | **Name** | `potatoshield-api` |
   | **Root Directory** | `backend` |
   | **Runtime** | `Python 3` |
   | **Build Command** | `pip install -r requirements.txt` |
   | **Start Command** | `uvicorn main:app --host 0.0.0.0 --port $PORT` |
   | **Plan** | `Free` |

5. Under **"Environment Variables"**, click "Add from .env" or add these manually:

   | Key | Value |
   |-----|-------|
   | `SECRET_KEY` | Click "Generate" (Render will make a secure one) |
   | `GOOGLE_CLIENT_ID` | `40176885150-ptp34v8v2g57t65bipj4r82qh7jlmq6b.apps.googleusercontent.com` |
   | `GOOGLE_CLIENT_SECRET` | `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` |
   | `FRONTEND_URL` | `https://potatoshield.vercel.app` |
   | `BACKEND_URL` | `https://potatoshield-api.onrender.com` |
   | `ALLOWED_ORIGINS` | `https://potatoshield.vercel.app` |

6. Click **"Create Web Service"**. Render will build and deploy. Copy the URL it gives you (e.g. `https://potatoshield-api.onrender.com`).

---

## Step 3 — Deploy Frontend to Vercel

1. Go to [vercel.com](https://vercel.com) and sign up with GitHub (free, no credit card)
2. Click **"Add New Project"** → import your `potatoshield` repo
3. Fill in the settings:

   | Setting | Value |
   |---------|-------|
   | **Framework Preset** | `Vite` |
   | **Root Directory** | `frontend` |

4. Under **"Environment Variables"**, add:

   | Key | Value |
   |-----|-------|
   | `VITE_API_URL` | `https://potatoshield-api.onrender.com` |

5. Click **"Deploy"**. Vercel will build the React app. Your site is now live at `https://potatoshield.vercel.app`.

---

## Step 4 — Update Google Cloud Console

> **Required for "Sign in with Google" to work.**

1. Go to [console.cloud.google.com](https://console.cloud.google.com) → **APIs & Services → Credentials**
2. Click your OAuth 2.0 Client ID
3. Under **Authorized redirect URIs**, add:
   ```
   https://potatoshield-api.onrender.com/auth/google/callback
   ```
4. Under **Authorized JavaScript origins**, add:
   ```
   https://potatoshield.vercel.app
   ```
5. Click **Save**.

---

## Step 5 — Verify

| Check | URL | Expected |
|-------|-----|----------|
| Frontend loads | `https://potatoshield.vercel.app` | Login page shows ✅ |
| API health | `https://potatoshield-api.onrender.com/docs` | FastAPI docs ✅ |
| Register / Login | Create account | Works ✅ |
| Predict | Upload a leaf image | Result returned ✅ |
| Google OAuth | Sign in with Google | Full round-trip works ✅ |

---

## How to Update the App Later

Whenever you make changes to the code, just push to GitHub:

```powershell
git add .
git commit -m "describe your change"
git push
```

- Vercel and Render **auto-detect the push** and redeploy automatically. No manual steps.

---

## Security ✅

- All traffic over **HTTPS** (Vercel and Render handle SSL automatically)
- CORS locked to `https://potatoshield.vercel.app` only
- Google OAuth secrets never exposed to the browser
- JWT secret auto-generated by Render
